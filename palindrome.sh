A shell program to find whether a number is palindrome or not.
#! /bin/bash
# Priyanshu Dhall , 24mca022
echo "Enter a number:"
read num
rev_num=$ (echo $num | rev)
if [
"$num"
echo
=
"$rev _num" ]; then
"'num is a Palindrome"
else
echo "$num is Not a Palindrome"
